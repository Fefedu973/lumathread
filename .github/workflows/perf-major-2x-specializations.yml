name: Major candidate selection placeholder

on:
  pull_request:
    branches:
      - perf/visual-parity-renderer-optimization
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: major-reliable-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  status:
    runs-on: ubuntu-latest
    steps:
      - name: Selection delegated
        run: echo "The corrected single-frame candidate matrix is running in PR 15."
